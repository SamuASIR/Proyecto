<html>
	<head>
		<title>Hoteles</title>
	</head>
	<body>
			{% if user.is_authenticated %}
				<p>Soy {{ user.get_username }}</p>
			{% endif %}
		<nav>
			<ul>
				{% if user.is_authenticated %}	
					<li><a href="{% url 'logout' %}">Logout</a></li>
				{% else %}
					<li><a href="{% url 'registro' %}">Registro</a></li>
					<li><a href="{% url 'login' %}">Login</a></li>
				{% endif %}
			</ul>
		</nav>
		{% if portada %}
			<table border="2">
				<tr>
					<td>nombre</td>
					<td>lugar</td>
					<td>direccion</td>
					<td>telefono</td>
					<td>web</td>
					<td>foto</td>
				{% if user.is_authenticated %}
					<td>Reserva</td>
				{% endif %}

				</tr>
				{% for pagina in portada %}
					<tr>
						<td>{{ pagina.nombre }}</td>
						<td>{{ pagina.lugar }}</td>
						<td>{{ pagina.direccion }}</td>
						<td>{{ pagina.telefono }}</td>
						<td><a href="{{pagina.web}}">web</a></td>
						<td><img src="{{pagina.foto}}"></td>
						{% if user.is_authenticated %}
						<td><a href="{% url 'reservahotel' %}">Reserva</a</td>
						{% endif %}
					</tr>
				{% endfor %}
			</table>
		{% else %}
			<p>hoooteeeel</p>			
		{% endif %}
	</body>
</html>

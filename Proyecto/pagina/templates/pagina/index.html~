{% load staticfiles %}
<html>
	<head>
		<title>Hoteles</title>
		<h1 id=cabecera>Hoteles en Aller</h1>
		<link rel="stylesheet" type="text/css" href="{% static "css/index.css" %}">
	</head>
	<body>
		<div id=menu>
			<h3>Menu</h3>
			{% if user.is_authenticated %}
				<p id=login>Soy {{ user.get_username }}</p>
			{% endif %}
		<nav>
			<ul>
				{% if user.is_authenticated %}	
					<li><a href="{% url 'logout' %}">Logout</a></li>
					<li><a href="{% url 'mis_reservas' %}">Mis reservas</a></li>
				{% else %}
					<li><a href="{% url 'registro' %}">Registro</a></li>
					<li><a href="{% url 'login' %}">Login</a></li>
				{% endif %}
			</ul>
		</nav>
		</div>
		{% if portada %}
			<table class=tabla border="2">
				<tr>
					<td>Nombre</td>
					<td>Lugar</td>
					<td>Direccion</td>
					<td>Telefono</td>
					<td>Web</td>
				{% if user.is_authenticated %}
					<td>Reserva</td>
				{% endif %}

				</tr>
				{% for hotel in portada %}
					<tr>
						<td><a href="{% url 'detalles' hotel.id %}">{{ hotel.nombre }}</a></td>
						<td>{{ hotel.lugar }}</td>
						<td>{{ hotel.direccion }}</td>
						<td>{{ hotel.telefono }}</td>
						<td><a href="{{hotel.web}}">web</a></td>
						{% if user.is_authenticated %}
						<td><a href="{% url 'reservahotel' hotel.id %}">Reserva</a</td>
						{% endif %}
					</tr>
				{% endfor %}
			</table>
		{% else %}
			<p>hoooteeeel</p>			
		{% endif %}
	</body>
</html>
